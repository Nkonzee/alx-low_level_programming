#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: provie 0 error for success run
 *
 */
int main(void)
{
	int c;

	c = 0;
	while (c < 10)
	{
		putchar('c');
		c++;
	}
	putchar(10);
	return (0);
}
